<%= turbo_frame_tag dom_id(mapping), class: "grid grid-cols-[50px_1fr_1fr_120px] gap-4 p-4 border-t hover:bg-gray-50 items-center" do %>
  <div><%= mapping.id %></div>
  <div><%= OrderFieldSourceKeys::SOURCE_KEYS[mapping.source_key] || mapping.source_key %></div>
  <div><%= mapping.insales_field_title.presence || mapping.insales_field_handle.presence || mapping.insales_field_id %></div>
  <div class="flex justify-end space-x-2">
    <%= link_to_edit edit_insale_insales_order_field_mapping_path(insale, mapping), data: { turbo_frame: "offcanvas", turbo_prefetch: false } %>
    <%= link_to_delete insale_insales_order_field_mapping_path(insale, mapping), data: { turbo_method: :delete, turbo_confirm: t("actions.delete") } %>
  </div>
<% end %>
