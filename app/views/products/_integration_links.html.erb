<div class="<%= wrapper_class %>">
  <% product.integration_links.each do |link| %>
    <%= link_to link.url, target: "_blank", class: "inline-flex items-center px-1.5 py-0.5 rounded text-xs font-medium cursor-pointer #{link.css}" do %>
      <%= link.label %>
    <% end %>
  <% end %>
</div>
