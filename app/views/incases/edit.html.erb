<div class="w-full">
  <div class="flex items-center mb-4 space-x-4">
    <%= link_to_back incases_path %>
    <h1 class="text-2xl font-semibold"><%= t('.heading') %></h1>
  </div>
  <%= render "form", incase: @incase %>
</div>

